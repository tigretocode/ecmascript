let hello = "Hello";
let world = "World";

let epicPhrase = hello + " " + world + "!";
console.log(epicPhrase);

//Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

//Multi-line strings
let lore = "esto es un string \n" + "estos es otra linea";

let lore2 = `Esta es una frase epica
la continuacion de esa frase epica.
`;
console.log(lore);
console.log(lore2);
